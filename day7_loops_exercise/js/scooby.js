//Cycle through array using For Loop

var names = ["Scooby","Shaggy","Velna", "Fred", "Daphne"];

//loop through array

for (var i=0; i<names.length; i++){

    console.log("You solved the case, " + names[i] + "!");
}